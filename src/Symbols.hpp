#ifndef SYMBOLSHEADERDEF
#define SYMBOLSHEADERDEF

namespace sim {

enum class Axes {
    x,
    y,
    z
};

enum class Octant {
    mmm,
    mmp,
    mpm,
    mpp,
    pmm,
    pmp,
    ppm,
    ppp
};

#endif
